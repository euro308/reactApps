export function SiteHeader() {
    return (

        <header className={"min-w-full flex items-center justify-center h-20 bg-red-700 text-white font-bold"}>
            <span className={"text-4xl"}>Blogovací Systém</span>
        </header>
    )
}