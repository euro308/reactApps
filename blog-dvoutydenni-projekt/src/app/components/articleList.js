const bob = sessionStorage.getItem(`name[0]`)
console.log(bob)

export function ArticleList() {
    return (
        <main className={"min-w-[80%] flex flex-col justify-center items-center border-2"}>
        </main>
    );
}
